{{template "admin" .}}

{{define "page-title"}}
    All Reservation 
{{end}}

{{define "content"}}
    <div class="col-md-12">
        {{$res := index .Data "reservation"}}

        {{range $res}}
            {{.ID}}<br>
        {{end}}
    </div>

{{end}}